{
  "name": "nr-browser-aggregator",
  "version": "1.0.0",
  "description": "This is a package that is deployed as a bundle to CDN. It is downloaded by the `agent-loader` package. These two packages are intended to work together.",
  "main": "src/index.js",
  "dependencies": {
    "nr-browser-common": "file:../common",
    "nr-browser-core": "file:../core",
    "nr-browser-err-aggregate": "file:../err-aggregate",
    "nr-browser-xhr-aggregate": "file:../xhr-aggregate",
    "webpack": "^5.65.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack && cp dist/nr-spa.js ../../build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack-cli": "^4.9.1",
    "eslint": "^8.8.0"
  }
}
